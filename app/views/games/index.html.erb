<%= render 'ui/navbar' %>

<div class="container">
  <% @days.each do |day, games| %>

    <%= render 'days/day', day: day %>
    
      <% games.each do |game| %>
        <div class="clearfix mxn2 mt3 mb3 table">
            <%= render 'games/game', game: game %>
          </div>
      <% end %>

  <% end %>
</div>
